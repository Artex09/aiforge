# Assets

Static assets for AIForge.

- `logo.svg` — the AIForge mark (self-contained SVG, no external references).

The dashboard's own static assets (HTML/CSS/JS) live under
`aiforge/dashboard/static/` and are served directly by the API layer.
